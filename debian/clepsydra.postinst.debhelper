# Automatically added by dh_installinit
if [ -x "/etc/init.d/clepsydra" ]; then
	update-rc.d clepsydra defaults >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d clepsydra start || exit $?
	else
		/etc/init.d/clepsydra start || exit $?
	fi
fi
# End automatically added section
